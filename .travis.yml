language: go

os:
  - linux
  - osx
  - windows

env:
  - GO111MODULE=on

go:
  - 1.14.x

install:
  - GO111MODULE=off go get -v github.com/codeclimate/test-reporter

before_script:
  - test-reporter before-build

script:
  - make test
  - GOOS=$TRAVIS_OS_NAME make dist

after_script:
  - test-reporter after-build --coverage-input-type gocov --exit-code $TRAVIS_TEST_RESULT
